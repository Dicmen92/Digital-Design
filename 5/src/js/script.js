const arr = new Array();
//const arr = [];
for (let i=0, t=20; i<t; i++) {
    arr.push(Math.round(Math.random() * t))    
}
document.write(arr);

//Массив получилось заполнить только случайными числами..